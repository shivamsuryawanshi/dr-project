// Cleanup utility for admin applications page
// Left blank or no-op since duplicate filter card is removed directly from component layout
export function scheduleCleanup() {
  // No-op: duplicate filter card was removed from template, mobile filters button preserved
}
